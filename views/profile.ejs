<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.6.0/jquery.min.js"></script>
    <link rel="stylesheet" href="<%= assetPath('css/profile.css') %>" type="text/css">
    <title><%= title %> </title>
</head>
<body>
     
    <div id="profile-pic">
        <img src="<%= user_profile.avatar %>" alt="<%= user_profile.name %>" width="100">
    </div>
    

    <% if(user.id == user_profile.id){ %> 

        
        <a href='about'>About 1</a>
        <a href='/about'>About 2</a>
        <!-- if we were currently on mywebsite.com/projects/generator, and we had the following links: -->

        <!-- <a href='about'>About 1</a> -->
        <!-- <a href='/about'>About 2</a> -->


        <!-- "About 1" would navigate to mywebsite.com/projects/about 
            (replacing the current path /generator) and
         "About 2" would navigate to mywebsite.com/about -->




        <form action="/users/update/<%= user.id %>" method="post" enctype="multipart/form-data">

            <input type="text" name="name" placeholder="name" value="<%= user.name %>" required>
            <input type="email" name="email" placeholder="email" value="<%= user.email %>" required>
            <input type="file" name="avatar" placeholder="profile picture">
            <input type="submit" value="update info">

        </form>

    <% }else{ %> 
        <p>name <%= user_profile.name %> </p>
        <p>email <%= user_profile.email %> </p>
        <button type="submit" value="<%= user_profile._id %>" id="friend-btn">
            <% if(locals.friendship){ %> 
                Remove
            <% }else{ %>
                Add friend
            <% } %> 
        </button>
    <% } %> 



    <p><a href="/users/signout">Sign-Out</a></p>
    <script src="<%= assetPath('/front_java/profile_front_java.js') %>" type="text/javascript">

    </script>
</body>
</html>